# TEMPLATE — do not apply this file directly. Copy to overlays/<target>/secrets.env,
# fill in values, and reference it from the overlay's kustomization.yaml:
#
#   secretGenerator:
#     - name: caytu-secrets
#       envs: [secrets.env]
#
# Kustomize handles base64 encoding + name suffixing.
#
# secrets.env format (KEY=VALUE per line):
#
# MINIO_ROOT_USER=minioadmin
# MINIO_ROOT_PASSWORD=<strong password>
# JWT_SECRET=<32+ bytes>
# TURN_SECRET=<32+ bytes>
# RECORDER_CALLBACK_SECRET=<32+ bytes>
# CALLBACK_SHARED_SECRET=<same as above>
# SIGNALING_AUTH_TOKEN=<uuid>
# ANTHROPIC_API_KEY=<key>
# OPENAI_API_KEY=<key>
# STRIPE_SECRET_KEY=<key>
# STRIPE_WEBHOOK_SECRET=<key>
# EMAIL_USER=<smtp user>
# EMAIL_PASSWORD=<smtp app password>
#
# For AWS overlays add:
# AWS_IOT_ENDPOINT=<endpoint>
# AWS_IOT_ROLE_ALIAS=<alias>
#
# Do NOT commit secrets.env — it's already gitignored under kubernetes/overlays/*/.
apiVersion: v1
kind: Secret
metadata:
  name: caytu-secrets-placeholder
type: Opaque
stringData:
  README: "This is a placeholder — see this file's comments."
